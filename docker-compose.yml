services:
  buoy-site:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: buoy-site
    restart: unless-stopped
    ports:
      - "3248:80"
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
